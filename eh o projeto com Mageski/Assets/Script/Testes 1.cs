using UnityEngine;

public class Testes1 : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("Quem eh esse neguinho?");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
