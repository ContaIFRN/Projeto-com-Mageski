using UnityEngine;

public class InimgoAe : SistemaDeVIda
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        vidaMax = 20;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
